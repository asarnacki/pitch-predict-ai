---
import Layout from "../layouts/Layout.astro";
import { AuthForm } from "../components/AuthForm";
import { Toaster } from "../components/ui/sonner";

// This page should be server-side rendered
export const prerender = false;

// TODO: When middleware is implemented, redirect logged-in users to home
// const user = Astro.locals.user;
// if (user) {
//   return Astro.redirect('/');
// }
---

<Layout title="Zarejestruj się - PitchPredict AI">
  <main class="min-h-screen flex items-center justify-center p-4 bg-background">
    <div class="w-full max-w-md">
      <AuthForm client:load mode="register" />
    </div>
  </main>
  <Toaster client:load />
</Layout>
